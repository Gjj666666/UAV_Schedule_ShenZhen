# 城市低空无人机智能调度与安全航线规划系统

## 部署及使用说明

**运行方式：** 本地浏览器访问的 Web 仿真系统 
**建议演示环境：** Windows 10/11、Python 3.9～3.12、Chrome 或 Edge、稳定网络

## 1. 项目简介

系统以深圳市为场景，基于 Cesium 三维地图、深圳建筑高度数据和分层 A* 路径规划，实现多无人机任务录入、批量调度、建筑与禁飞区避障、能耗与换电规划、天气管控、在途异常处置、实时航迹显示和飞行日志留存。

核心数据及能力包括：

- 深圳约 74.5 万栋建筑的 WGS84 Shapefile 与 `Height` 高度字段；
- 10 个无人机基地、30 个换电站及多架无人机并行调度；
- 绝对禁飞区与需审批管制区；
- 单任务、表格批量任务和自然语言 AI 任务录入；
- 天气暂停派遣、低电量换电、异常返航或携货接驳；
- 实时调度进度、完整规划路线、已飞轨迹和任务飞行日志。

## 2. 运行环境

### 2.1 必需环境

- Windows 10/11（推荐；macOS、Linux 也可使用等价命令）；
- Python 3.9～3.12；
- Chrome 或 Edge 浏览器；
- 可访问 Cesium 服务的网络。

### 2.2 必须保留的项目文件

请完整保留项目目录，尤其不要删除：

- `data/shenzhen_buildings/`：深圳建筑 Shapefile 与空间索引；
- `web/`：Cesium 前端页面；
- `requirements.txt`：Python 依赖；
- `start.py`：一键启动入口；
- 根目录下的各类 `*_config.py` 配置文件。

## 3. 首次部署

拉取项目，执行：

```powershell
python -m pip install -r requirements.txt
python start.py
```



看到服务启动信息后，在浏览器打开：

```text
http://localhost:8080
```



演示结束后，在启动窗口按 `Ctrl+C` 停止服务。以后再次运行只需激活虚拟环境并执行 `python start.py`。

## 4. 可选服务配置

项目的核心调度、坐标任务导入和仿真功能可独立运行；地点搜索、AI 解析、真实天气和 Cesium 在线地图需要相应服务。

| 配置文件 | 配置项 | 用途 |
|---|---|---|
| `web/config.js` | `cesiumIonToken` | Cesium 影像、地形及相关在线服务 |
| `app_config.py` | `AMAP_WEB_SERVICE_KEY` | 高德地点检索和深圳实时天气 |
| `app_config.py` | `OPENAI_API_KEY`、`OPENAI_MODEL`、`OPENAI_BASE_URL` | OpenAI 兼容大模型的自然语言任务解析 |
| `weather_config.py` | `USE_REAL_WEATHER` | `True` 使用真实天气，`False` 使用模拟天气 |
| `weather_config.py` | `SIMULATED_WEATHER` 等 | 设置晴、雨、风力、温湿度等模拟状态 |
| `energy_config.py` | 能耗、余量、换电/充电时间等变量 | 调整百公里耗电和能源保障策略 |

填写或修改配置后需重启服务。请勿把无限制的正式密钥公开提交；比赛演示 Key 建议设置域名/IP、调用额度和有效期限制。

## 5. 启动后的快速检查

页面打开后确认：

1. 右下角显示“调度服务器已连接”；
2. 地图能够定位到深圳，基地、换电站和空域区域可见；
3. “建筑数据集”显示可用；
4. 右侧天气卡显示模拟或实时天气状态；
5. 如需使用地点名称，点击“检测高德调用”，以“高德实际调用成功”为准。

若只使用带 WGS84 经纬度的样例文件，则不依赖高德地点解析。

## 6. 基本使用方法

### 6.1 初始化无人机

在左侧“无人机机队”中：

1. 设置无人机数量，默认 6 架；
2. 选择仿真速度，建议展示使用 `4×` 或 `10×`；
3. 点击“初始化机队”。

无人机会出现在所属基地的安全起降点。无人机数量少于任务数时，多余任务进入等待队列，并在无人机空闲后继续调度。

### 6.2 新建单个任务

有三种输入方式：

- 在“手动新建配送任务”中输入地点名称；
- 直接输入 `经度,纬度`；
- 点击“地图选点”。

选择候选地点并填写配送类型、优先级、重量、时限和首选航高后，点击“解析地点并加入任务队列”，再点击“开始调度”。

### 6.3 批量导入任务（推荐比赛演示）

1. 在“批量导入飞行任务”中选择：`samples/深圳20条地点批量飞行任务.csv`；
2. 点击“解析表格文件”；
3. 查看路线、参数和安全校验预览；
4. 点击“确认并批量初始化有效任务”；
5. 点击“开始调度”。

该样例已填写 WGS84 经纬度，可避免地点接口限流。系统支持 XLSX、CSV、JSON，文件批量单次最多 200 条；AI 批量识别最多 50 条。

再次测试前建议先点击“重置”，否则新任务会追加到现有队列。重置会清空当前任务和无人机运行状态，不会删除建筑库或内置空域数据。

### 6.4 AI 自然语言录入

在“AI 智能任务助手”输入，例如：

```text
从深圳北站送2公斤急救药品到深圳人才公园，30分钟内送达，优先级紧急。
```

依次点击“AI 理解任务”和“确认并创建任务”。大模型只生成结构化任务草案；地点坐标仍通过地图/地点服务确认，最终路线仍由原有安全规则和规划器计算。

### 6.5 查看路线与跟随视角

地图图例区分“起飞→取货”“取货→终点”和“已飞轨迹”，并显示无人机基地、换电站、绝对禁飞区及审批管制区。右侧“实时调度路线”同步显示承运无人机、分段里程、换电点、航层、路线版本、重规划原因和航段进度。

在“视角跟随无人机”下拉框或右侧无人机列表选择一架无人机后：

- 左键拖动：360° 环绕观察；
- 鼠标滚轮：放大或缩小；
- 点击“取消跟随”：恢复自由视角。

### 6.6 空域审批

右侧“禁飞区与空域审批”可调整管制区状态：

- 绝对禁飞区：始终禁止进入，不能通过审批放行；
- 审批管制区：选择“已审批”后允许参与路线规划，选择“未审批”时按障碍处理。

状态变化后，后续调度或重规划会使用新的空域约束。

### 6.7 天气与异常演示

右侧天气卡可查看当前天气及“允许派遣/暂停新派遣/应急撤离”状态。模拟模式下可选择天气预设并立即应用。

左侧“飞行事件模拟”可对正在飞行的无人机触发：

- 阵风：无人机降速并复核能耗；
- 通信丢失：短时悬停，恢复后继续；
- 低电量：重新评估并规划换电或安全处置；
- 临时禁飞区：触发实时重规划。

如已携货无人机无法安全继续，系统会选择可达的基地或换电站作为交接点，安排后续无人机接驳；未取货时可中止并返航。完成配送后，无人机会返回合适基地，低电量时可在基地充电。

## 7. 推荐的 5 分钟演示流程

1. 打开系统，展示深圳三维地图、基地、换电站、建筑和禁飞区；
2. 点击“重置”，初始化 6 架无人机，仿真速度设为 `10×`；
3. 导入 `samples/深圳30条地点批量飞行任务.csv` 并批量初始化；
4. 点击“开始调度”，观察多机并行执行、等待队列和调度进度条；
5. 在地图与“实时调度路线”中展示取货段、配送段、换电点和路径动态更新；
6. 跟随一架无人机，演示 360° 视角和缩放；
7. 触发一次“低电量”或“临时禁飞区”，展示换电/重规划；
8. 切换一次恶劣模拟天气，展示暂停新派遣或在途应急，再恢复晴天；
9. 等待任务完成，在“已完成飞行日志”中查看 JSON 明细或下载 CSV 汇总。

## 8. 运行结果与日志

系统运行时会自动创建 `runtime/`，主要内容包括：

- `runtime/flight_logs/TASK-XXXXXX.json`：单任务详细日志；
- `runtime/flight_logs/flight_summary.csv`：已完成任务汇总；
- 运行时路线、缓存和其他临时状态文件。

单任务日志记录任务参数、参与无人机、天气、路线版本、轨迹采样点、异常事件、换电次数、航程、高度和电量等信息。`runtime/` 属于运行生成内容，删除后会丢失历史记录，但不会影响源代码和建筑数据。

## 9. 常见问题

| 现象 | 处理方法 |
|---|---|
| 无法打开 `localhost:8080` | 确认 `python start.py` 窗口仍在运行；检查 8080 端口是否被占用。 |
| 地图空白或 Cesium 加载失败 | 检查网络与 `web/config.js` 中的 Token；确认 Token 未过期且授权范围正确。 |
| 建筑数据不可用 | 确认 `data/shenzhen_buildings/` 内 `.shp`、`.shx`、`.dbf` 和 `buildings_index.sqlite` 完整。 |
| 高德提示 `10021` 或 QPS 超限 | 等待额度恢复；比赛演示优先使用已带坐标的 30 条样例文件。 |
| AI 解析不可用 | 检查 `app_config.py` 的 AI Key、模型和 Base URL；手动/表格任务不受影响。 |
| 恶劣天气下一直不派遣 | 这是天气安全门控；恢复模拟晴天或等待真实天气恢复后继续。 |
| 批量任务数量比预期多 | 导入会追加任务；新一轮演示前先点击“重置”。 |
| 首次规划稍慢 | 首次需读取建筑空间索引并生成路线；观察调度进度条，后续相同路线可利用缓存加速。 |
| 个别任务长期等待 | 查看右侧原因，通常为暂无空闲无人机、载重/电量不足、天气暂停或无安全路线。 |

## 10. 演示前检查清单

- [ ] Python 依赖已安装，`python start.py` 可正常运行；
- [ ] 浏览器可打开 `http://localhost:8080`；
- [ ] Cesium 地图和深圳建筑数据加载正常；
- [ ] 初始化无人机后，地图能看到基地与机队；
- [ ] 30 条样例任务可以解析并通过预览；
- [ ] 天气设为可飞状态，空域审批状态符合演示方案；
- [ ] 已预演一次低电量、临时禁飞区或恶劣天气事件；
- [ ] 正式演示前点击“重置”，避免旧任务影响展示。

